# Clone into ~, then link files
cd ~
ln -s dotfiles/.bashrc_flavius
ln -s dotfiles/.vimrc 
ln -s dotfiles/.gvimrc 
ln -s dotfiles/.hgrc 
ln -s dotfiles/.gitconfig 
ln -s dotfiles/.vim

# Edit .bashrc, .profile or whatever
# Add
source ~/.bashrc_flavius
